interface:
  display_name: SSH Ops
  short_description: Core SSH tools for remote server operations.
  brand_color: "#10B981"
  default_prompt: Run a health check on my production server with SSH Ops.
policy:
  allow_implicit_invocation: true
